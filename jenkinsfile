pipeline {
    agent any

    stages {
        stage('Automation Test') {
            steps {
                echo "this is automation test :)"
            }
        }
    }

    post {
        always {
            echo "Pipeline finished!!!"
        }
    }
}
