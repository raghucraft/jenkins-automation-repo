pipeline {
    agent any

    stages {
        stage('Automation Test') {
            steps {
                echo "this is automation test V :)"
            }
        }
    }

    post {
        always {
            echo "Pipeline finished!!!!"
        }
    }
}
